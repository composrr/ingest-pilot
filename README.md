# Ingest Pilot

Ingest Pilot is a desktop app for repeatable project folder scaffolding, verified media ingest, and token-based rename-on-import workflows.

## Development

```powershell
npm.cmd install
npm.cmd run tauri:dev
```

## Build

```powershell
npm.cmd run tauri:build
```

The first build will download JavaScript and Rust dependencies.
